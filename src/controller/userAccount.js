import pool from '../model/db';

import UserTransaction from '../controller/userTransactions';

class UserAccount extends UserTransaction {
    construction(accountBalance, transactions) {
        super(fullName, income, expenses);
        this.accountBalance = accountBalance;
        this.transactions = transactions;
    }


}

export default UserAccount;